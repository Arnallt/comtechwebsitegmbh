StylesPath = styles
MinAlertLevel = warning

# Parse .mdx as Markdown — avoids the mdx2vast dependency. Our MDX bodies are
# plain prose; structured copy lives in frontmatter (linted below).
[formats]
mdx = md

[*.{md,mdx}]
BasedOnStyles = ComTech
