---
import { getEntry } from 'astro:content';
import StandardPage from '../layouts/StandardPage.astro';

const entry = await getEntry('pages', 'home');
if (!entry) throw new Error('Missing pages/home.mdx');
---

<StandardPage entry={entry} />
